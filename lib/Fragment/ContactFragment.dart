import 'package:flutter/material.dart';

class ContactFragment extends StatelessWidget {
  const ContactFragment({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Center(
        child: Text("This is my contact fragment"),
      ),
    );
  }
}

import 'package:flutter/material.dart';

class PersonFragment extends StatelessWidget {
  const PersonFragment({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Center(
        child: Text("This is my PersonFragment"),
      ),
    );
  }
}

import 'package:flutter/material.dart';

class BalanceFragment extends StatelessWidget {
  const BalanceFragment({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Center(
        child: Text("This is my balance fragment"),
      ),
    );
  }
}

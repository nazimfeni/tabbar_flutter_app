import 'package:flutter/material.dart';

class EmailFragment extends StatelessWidget {
  const EmailFragment({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Center(
        child: Text("This is my EmailFragment"),
      ),
    );
  }
}
